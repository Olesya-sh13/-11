# Отчёт по лабораторной работе №11
## Качество программного обеспечения

---

**Дисциплина:** Системная и программная инженерия  
**Тема:** Качество программного обеспечения  
**Программное средство:** Контейнер журналирования событий доступа (Security Logger)  

---

## 1. Цель работы

Получить навыки оценки качества программного обеспечения на примере контейнера для обеспечения безопасности приложения. Рассмотреть процесс менеджмента качества по ГОСТ Р ИСО/МЭК 12207, провести сравнительный анализ систем мониторинга и подготовить отчёт по модели качества согласно ГОСТ Р ИСО/МЭК 25010.

---

## 2. Формулировка задания

Для программного средства — **Контейнера журналирования событий доступа** (Security Logger, реализованного на Django 5.0.2) — необходимо:

1. Рассмотреть процесс менеджмента качества по ГОСТ Р ИСО/МЭК 12207, определить политики, стандарты и процедуры в области менеджмента качества.
2. Провести сравнительный анализ 5–6 систем мониторинга для оценки качества ПО по критериям: функциональность, стоимость, надёжность и удобство использования.
3. Подготовить отчёт по модели качества согласно ГОСТ Р ИСО/МЭК 25010, включающий 8 разделов.
4. Настроить модель реализации качества с помощью выбранной системы мониторинга.

---

## 3. Результаты выполнения задания

### 3.1 Оценка процесса менеджмента качества по ГОСТ Р ИСО/МЭК 12207

ГОСТ Р ИСО/МЭК 12207-2010 устанавливает общую структуру процессов жизненного цикла программных средств. Применительно к Security Logger рассматриваются следующие процессы.

#### 3.1.1 Применимые процессы жизненного цикла

**Процессы соглашения:**
- *Процесс приобретения* — определение требований к системе мониторинга безопасности. Для Security Logger требования сформулированы в ТЗ: контроль доступа к виртуальным машинам, обнаружение аномалий, шифрование логов.
- *Процесс поставки* — передача готового программного продукта заказчику через платформу Replit с автоматизированным скриптом развёртывания `setup.sh`.

**Процессы организационного обеспечения:**
- *Процесс менеджмента модели жизненного цикла* — применяется итерационная модель: функциональные требования → реализация → тестирование → развёртывание.
- *Процесс менеджмента инфраструктуры* — использование Python 3.10, Django 5.0.2, SQLite, Gunicorn, библиотек cryptography, pandas, numpy.
- *Процесс менеджмента человеческих ресурсов* — распределение ролей: разработчик (реализация бизнес-логики), администратор БД (миграции), тестировщик (pytest, модульные тесты).

**Процессы технического управления:**
- *Процесс планирования проекта* — определены сроки разработки, список артефактов: исходный код, документация, тесты, скрипт развёртывания.
- *Процесс управления рисками* — идентифицированы риски: компрометация ключа шифрования Fernet, утечка данных в открытых логах, SQL-инъекции. Контрмеры: хранение ключа в `secret.key`, шифрование `.enc`-файлов, использование Django ORM.

**Технические процессы:**
- *Процесс анализа требований* — требования к функциональности: ролевая модель (admin/auditor), мониторинг 4 ВМ, обнаружение частотных аномалий, настройка параметров политики.
- *Процесс проектирования архитектуры* — многоуровневая архитектура MVC: модели (models.py), представления (views.py), шаблоны (templates/), модуль шифрования (encryption.py), модуль симуляции (data/utils.py).
- *Процесс реализации* — разработка на Python/Django, Fernet-шифрование событий безопасности, токен-based аутентификация (обход ограничений cookies в iframe).
- *Процесс тестирования* — модульные тесты (pytest): test_encryption.py, test_models.py, test_views.py.
- *Процесс развёртывания* — автоматизированный скрипт `setup.sh`, Gunicorn на порту 5000.

#### 3.1.2 Политики, стандарты и процедуры менеджмента качества

| Область | Политика | Стандарт | Процедура |
|---------|----------|----------|-----------|
| Безопасность данных | Все критические события шифруются перед записью | ГОСТ Р ИСО/МЭК 27001 | Использование Fernet (AES-128-CBC + HMAC-SHA256); ключ хранится отдельно от данных |
| Управление доступом | Доступ к ресурсам регулируется ролевой моделью | ГОСТ Р ИСО/МЭК 25010 (безопасность) | Декораторы `@token_required`, `@admin_required`; policy.json определяет права read/write/none |
| Обнаружение аномалий | Превышение базовой частоты запросов = аномалия | ГОСТ Р ИСО/МЭК 25010 (надёжность) | Подсчёт запросов за скользящее окно 1 сек; критический порог = ⌈base × (1 + overhead/100)⌉ |
| Тестирование | Каждый модуль покрыт автотестами | ГОСТ Р ИСО/МЭК 12207 (тестирование) | pytest + pytest-django; round-trip тесты шифрования, параметризованные тесты моделей |
| Развёртывание | Повторяемое, автоматизированное развёртывание | ГОСТ Р ИСО/МЭК 12207 (поставка) | Скрипт setup.sh: миграции → пользователи → ключ → запуск Gunicorn |
| Аудит изменений | Все изменения политики фиксируются в audit.log | ГОСТ Р ИСО/МЭК 27001 | POST /logger/settings/ записывает: время, пользователь, старые/новые параметры |

---

### 3.2 Сравнительный анализ систем мониторинга для оценки качества ПО

#### Таблица 1 — Сравнительный анализ систем мониторинга для оценки качества ПО

| № | Система | Функциональность | Стоимость | Надёжность | Удобство использования |
|---|---------|-----------------|-----------|------------|----------------------|
| 1 | **Prometheus** | Сбор и хранение метрик временных рядов; интеграция с Kubernetes; поддержка alerting rules; экспортёры для Python (prometheus_client); мощный язык запросов PromQL; подходит для мониторинга HTTP-эндпоинтов и частоты запросов | Бесплатно (open-source); стоимость зависит от провайдера облачных услуг для хостинга; дополнительные расходы на хранилище при больших объёмах метрик | Высокая: pull-модель сбора данных обеспечивает работу при частичных отказах; встроенное локальное хранилище TSDB; поддержка federation и remote storage [1] | Гибкая настройка через YAML; крутая кривая обучения для PromQL; отсутствие встроенного UI для дашбордов (требует Grafana) |
| 2 | **Grafana** | Визуализация данных из множества источников (Prometheus, InfluxDB, Elasticsearch); создание интерактивных дашбордов; alerting; плагины для аномалий; Grafana Loki для логов — применимо для анализа логов Security Logger | Grafana OSS — бесплатно; Grafana Cloud — от $0 (бесплатный tier) до $299/мес; Enterprise — по запросу; self-hosted — только расходы на инфраструктуру [2] | Высокая при self-hosted развёртывании; облачный вариант гарантирует 99,9% SLA; активное community обеспечивает быстрое устранение уязвимостей | Интуитивный drag-and-drop интерфейс дашбордов; большая библиотека готовых шаблонов; хорошая документация на русском языке |
| 3 | **Zabbix** | Комплексный мониторинг серверов, сетей, приложений; агенты для Python; поддержка SNMP, JMX, IPMI; встроенные триггеры и эскалации; шаблоны для Django-приложений; подходит для мониторинга доступности Security Logger | Полностью бесплатно (open-source, AGPL); коммерческая поддержка от $4 500/год; отсутствие лицензионных сборов за узлы мониторинга — ключевое преимущество [3] | Очень высокая: proxy-архитектура обеспечивает отказоустойчивость; встроенная репликация БД; используется в критически важных производственных системах с 2001 года | Сложная первоначальная настройка; устаревший UI (улучшен в версии 6.x); обширная документация; активное русскоязычное сообщество |
| 4 | **Nagios** | Мониторинг серверов, сервисов и приложений; активная и пассивная проверка; уведомления по email/SMS; плагины для HTTP-мониторинга (check_http для эндпоинтов Security Logger); поддержка кастомных скриптов | Nagios Core — бесплатно (GPL); Nagios XI — от $1 995 единовременно или $495/год; XI предоставляет расширенные UI и отчёты | Высокая: зрелое ПО (с 1999 года); надёжная система оповещений; однако монолитная архитектура ограничивает масштабируемость по сравнению с современными решениями [4] | Nagios Core: устаревший UI, сложная конфигурация через текстовые файлы; Nagios XI: значительно улучшенный веб-интерфейс с мастерами настройки |
| 5 | **Datadog** | Полнофункциональная APM-платформа; распределённая трассировка; логирование; мониторинг безопасности (SIEM); интеграция с Python через ddtrace; Real User Monitoring; прямое применение к Security Logger для трассировки Django-запросов | Платная SaaS-платформа: инфраструктурный мониторинг от $15/хост/мес; APM от $31/хост/мес; Log Management от $0,10/ГБ; нет self-hosted варианта [5] | Очень высокая: 99,98% SLA; глобальная облачная инфраструктура; автоматическое масштабирование; статус доступности на status.datadoghq.com | Превосходный современный UI; быстрое подключение через агент; обширная документация; высокая стоимость ограничивает применение для учебных проектов |
| 6 | **New Relic** | APM с автоинструментированием Django; мониторинг производительности БД (SQLite запросы); error tracking; Synthetic мониторинг эндпоинтов; AI-assisted alerting; 700+ интеграций; прямое применение к Security Logger через newrelic.ini | Freemium: 100 ГБ/мес данных бесплатно, 1 пользователь; Full Platform: от $49/пользователь/мес; Data Plus: от $0,50/ГБ сверх лимита [6] | Высокая: мультирегиональное облачное развёртывание; 99,95% SLA; встроенный disaster recovery | Отличный UX; автоматическое обнаружение компонентов Django; подробные руководства по настройке; более простая настройка по сравнению с self-hosted решениями |

**Источники:**
1. Prometheus Documentation. https://prometheus.io/docs/introduction/overview/
2. Grafana Labs. Pricing. https://grafana.com/pricing/
3. Zabbix LLC. Zabbix Overview. https://www.zabbix.com/zabbix_overview
4. Nagios Enterprises. Nagios XI vs Core. https://www.nagios.com/products/nagios-xi/
5. Datadog Inc. Pricing. https://www.datadoghq.com/pricing/
6. New Relic Inc. Pricing. https://newrelic.com/pricing

#### Вывод по сравнительному анализу

Для мониторинга качества Security Logger оптимальным выбором является связка **Prometheus + Grafana**:
- Бесплатная и open-source, что соответствует учебному контексту проекта на Replit
- Prometheus собирает метрики HTTP-запросов (количество запросов/сек, коды ответов, время отклика) — напрямую соответствует логике обнаружения аномалий в приложении
- Grafana визуализирует динамику событий, аналогичную встроенному графику «Динамика обращений по времени»
- `prometheus_client` для Python интегрируется в Django за 15 минут

---

### 3.3 Отчёт по модели качества для программного средства (ГОСТ Р ИСО/МЭК 25010)

#### 3.3.1 Модель измерения качества программного средства

ГОСТ Р ИСО/МЭК 25010-2015 определяет два взаимосвязанных вида качества программного продукта:

**Модель качества продукта** включает 8 характеристик:

```
Качество продукта (ГОСТ Р ИСО/МЭК 25010)
│
├── 1. Функциональная пригодность
│   ├── Функциональная полнота
│   ├── Функциональная корректность
│   └── Функциональная целесообразность
│
├── 2. Эффективность выполнения
│   ├── Временна́я эффективность
│   ├── Использование ресурсов
│   └── Пропускная способность
│
├── 3. Совместимость
│   ├── Сосуществование
│   └── Взаимодействие
│
├── 4. Удобство использования
│   ├── Распознаваемость пригодности
│   ├── Простота обучения
│   ├── Управляемость
│   ├── Защита от ошибок пользователя
│   ├── Эстетика пользовательского интерфейса
│   └── Доступность
│
├── 5. Надёжность
│   ├── Зрелость
│   ├── Доступность
│   ├── Устойчивость к отказам
│   └── Восстанавливаемость
│
├── 6. Защищённость (безопасность)
│   ├── Конфиденциальность
│   ├── Целостность
│   ├── Неотказуемость
│   ├── Подотчётность
│   └── Аутентичность
│
├── 7. Сопровождаемость
│   ├── Модульность
│   ├── Повторное использование
│   ├── Анализируемость
│   ├── Изменяемость
│   └── Тестируемость
│
└── 8. Переносимость
    ├── Адаптируемость
    ├── Простота установки
    └── Заменяемость
```

**Модель качества при использовании** включает 5 характеристик: результативность, производительность, удовлетворённость, свобода от риска, охват контекста.

#### 3.3.2 Подходы к качеству

Для Security Logger применяются следующие подходы к обеспечению качества:

| Подход | Описание | Реализация в Security Logger |
|--------|----------|------------------------------|
| **Качество процессов** | Обеспечение качества через соблюдение стандартов разработки | Структурирование кода по MVC; разделение ответственности (encryption.py, utils.py, views.py); документирование через docstrings |
| **Качество продукта** | Соответствие ПО функциональным и нефункциональным требованиям | Ролевая модель доступа; шифрование логов; обнаружение аномалий частоты; настраиваемые параметры политики |
| **Качество при использовании** | Степень достижения пользователями своих целей | Интуитивный UI с тёплой цветовой палитрой; адаптивная вёрстка; пояснения к каждому элементу интерфейса |
| **Качество данных** | Точность и полнота обрабатываемых данных | Валидация входных параметров (min/max для числовых полей); шифрование критичных данных; целостность policy.json |

#### 3.3.3 Факторы, влияющие на качество

**Внутренние факторы (определяются на этапе разработки):**

- *Архитектурные решения:* Токен-based аутентификация вместо cookies обеспечивает совместимость с iframe-окружением Replit; разделение логики симуляции и UI позволяет независимо изменять алгоритмы без правки шаблонов.
- *Алгоритм обнаружения аномалий:* Скользящее окно 1 секунда с порогами base_frequency и critical_threshold; параметры вынесены в policy.json для гибкой настройки без перекомпиляции.
- *Шифрование:* Fernet (AES-128-CBC + HMAC-SHA256) гарантирует конфиденциальность и целостность логов; ключ хранится в отдельном файле.
- *Покрытие тестами:* Параметризованные тесты шифрования (round-trip), тесты модели пользователя, интеграционные тесты HTTP-представлений.

**Внешние факторы (среда эксплуатации):**

- *Платформа:* Replit (NixOS, Python 3.10, ограниченные ресурсы контейнера) — влияет на производительность при больших объёмах событий.
- *Браузер пользователя:* Iframe-ограничения требуют обходного механизма передачи токена через URL.
- *Gunicorn:* 1 worker process; при высокой нагрузке может стать узким местом.

#### 3.3.4 Модель жизненного цикла качества

```
Планирование качества
  │  ТЗ: ролевая модель, мониторинг ВМ, шифрование логов
  │  Метрики: покрытие тестами ≥ 80%, время отклика < 2 сек
  ▼
Обеспечение качества
  │  Code review; статический анализ (ruff); документирование кода
  │  Соответствие стандартам: ГОСТ 12207, ГОСТ Р ИСО/МЭК 25010
  ▼
Контроль качества
  │  Запуск pytest (test_encryption, test_models, test_views)
  │  Ручное тестирование UI: вход, настройки, журнал, шифрованные логи
  ▼
Улучшение качества
  │  Исправление ошибок (например, порядок декораторов в settings_view)
  │  Оптимизация (объединение временных рядов на графике)
  ▼
Развёртывание
     Скрипт setup.sh; Gunicorn; мониторинг через Prometheus (план)
```

#### 3.3.5 Свойства программного средства

Применительно к Security Logger выявлены следующие измеримые свойства качества:

| Характеристика ГОСТ 25010 | Свойство ПС | Показатель | Целевое значение |
|--------------------------|-------------|------------|-----------------|
| Функциональная полнота | Реализация всех функций из ТЗ | Доля реализованных требований | 100% (8/8 функций ТЗ) |
| Функциональная корректность | Правильность применения политики доступа | Доля корректных решений policy_check | 100% (покрыто тестами) |
| Временна́я эффективность | Время генерации отчёта на дашборде | Время отклика GET /logger/ | < 2 сек при 120 событиях |
| Конфиденциальность | Шифрование критичных событий | Доля зашифрованных инцидентов | 100% (все .enc файлы) |
| Целостность | HMAC-SHA256 в Fernet | Обнаружение модификации | Ошибка при изменении зашифрованных данных |
| Подотчётность | Аудит изменений политики | Наличие записи в audit.log | Каждое изменение settings |
| Зрелость (надёжность) | Работа без сбоев | MTBF (среднее время между отказами) | Непрерывная работа ≥ 24 часа |
| Анализируемость | Наличие комментариев в коде | Плотность комментариев | ≥ 1 комментарий на 5 строк кода |
| Тестируемость | Покрытие автотестами | Процент покрытия кода | ≥ 80% по pytest-cov |
| Простота установки | Автоматизация развёртывания | Количество ручных шагов | 1 (запуск ./setup.sh) |

#### 3.3.6 Показатели внутреннего, внешнего качества и качества при использовании

**Внутреннее качество** (статические свойства кода):

| Подхарактеристика | Метрика | Значение | Оценка |
|------------------|---------|----------|--------|
| Модульность | Количество независимых модулей | 5 (models, views, encryption, utils, urls) | Высокая |
| Повторное использование | Использование стандартных библиотек | Django ORM, Fernet, pandas, numpy | Хорошее |
| Изменяемость | Параметры политики в JSON | policy.json вынесен отдельно | Высокая |
| Тестируемость | Наличие тестов | 3 тестовых модуля, pytest fixtures | Хорошая |

**Внешнее качество** (поведение системы во время выполнения):

| Подхарактеристика | Метрика | Значение | Оценка |
|------------------|---------|----------|--------|
| Функциональная корректность | Тесты policy check | 100% корректных решений | Отлично |
| Временна́я эффективность | Время отклика дашборда | ~0.3–0.8 сек (120 событий) | Хорошо |
| Защищённость | Тест CSRF для settings | POST без токена → 403 Forbidden | Отлично |
| Доступность | Uptime Gunicorn | Непрерывно после `./setup.sh` | Хорошо |

**Качество при использовании:**

| Характеристика | Метрика | Значение |
|---------------|---------|----------|
| Результативность | Достижение цели (просмотр статистики) | 2 клика от входа до дашборда |
| Удовлетворённость | Ясность UI | Пояснения к каждому элементу; уведомление о сохранении |
| Свобода от риска | Защита от ошибок | Ролевое ограничение (auditor не видит настройки) |

#### 3.3.7 Взаимосвязь качества программного средства и качества данных

В Security Logger данные и программный код тесно взаимосвязаны:

**policy.json — центральный файл данных:**
- *Точность данных:* Некорректные значения base_frequency или minor_overhead_percent непосредственно влияют на правильность обнаружения аномалий. Решение: валидация на уровне HTML (min/max атрибуты) и в views.py (приведение к int).
- *Целостность данных:* Файл перезаписывается атомарно через `json.dump()` с `indent=2`. При сбое записи — данные сохраняют прежнее состояние.
- *Согласованность данных:* Изменения policy.json немедленно влияют на следующий вызов `generate_events()`, поскольку файл перечитывается при каждом запросе.

**Зашифрованные логи (.enc файлы):**
- *Конфиденциальность данных → защищённость ПС:* Компрометация ключа `secret.key` нарушает как качество данных (читаемость логов посторонними), так и безопасность системы.
- *Целостность данных → надёжность ПС:* HMAC-SHA256 в Fernet обнаруживает любую модификацию зашифрованного файла, гарантируя, что отображаемые администратору данные не были подделаны.

**Симулируемые события (pandas DataFrame):**
- Качество симуляции (coverage нормальных/аномальных сценариев) определяет качество статистики на дашборде. Все 5 типов событий (злоумышленник, нарушения политики, малые аномалии, критические аномалии, нормальные события) покрыты в `generate_events()`.

#### 3.3.8 Приложение. Настройка модели реализации качества с помощью Prometheus

**Выбранная система мониторинга: Prometheus + Grafana**

Обоснование: бесплатная, open-source, нативная интеграция с Python/Django через `prometheus_client`, поддерживает метрики временны́х рядов (запросы/сек), что прямо соответствует логике обнаружения аномалий в Security Logger.

**Шаг 1. Установка зависимостей**

```bash
poetry add prometheus-client
```

**Шаг 2. Создание модуля метрик**

Файл `logger_app/metrics.py`:

```python
"""
Метрики Prometheus для Security Logger.
Экспортирует показатели качества согласно ГОСТ Р ИСО/МЭК 25010.
"""
from prometheus_client import Counter, Histogram, Gauge

# Характеристика: Функциональная пригодность → Функциональная полнота
# Метрика: общее количество проверок политики доступа
POLICY_CHECKS_TOTAL = Counter(
    'security_logger_policy_checks_total',
    'Общее количество проверок политики доступа',
    ['result']  # labels: allowed / denied
)

# Характеристика: Эффективность выполнения → Временна́я эффективность
# Метрика: время обработки запроса к дашборду
REQUEST_DURATION = Histogram(
    'security_logger_request_duration_seconds',
    'Время обработки HTTP-запросов',
    ['endpoint'],
    buckets=[0.1, 0.25, 0.5, 1.0, 2.0, 5.0]
)

# Характеристика: Надёжность → Зрелость
# Метрика: количество аномалий за последний цикл симуляции
ANOMALY_COUNT = Gauge(
    'security_logger_anomaly_count',
    'Количество аномалий в последнем цикле симуляции',
    ['type']  # labels: frequency / critical
)

# Характеристика: Защищённость → Подотчётность
# Метрика: количество изменений политики безопасности
POLICY_CHANGES_TOTAL = Counter(
    'security_logger_policy_changes_total',
    'Количество изменений параметров политики безопасности администратором'
)
```

**Шаг 3. Интеграция метрик в views.py**

```python
# В log_dashboard():
from .metrics import REQUEST_DURATION, ANOMALY_COUNT
import time

@token_required
def log_dashboard(request):
    start = time.time()
    data = run_security_simulation()
    
    # Обновляем метрики качества
    ANOMALY_COUNT.labels(type='frequency').set(data['anomaly_events'])
    ANOMALY_COUNT.labels(type='critical').set(data['critical_anomaly_events'])
    REQUEST_DURATION.labels(endpoint='dashboard').observe(time.time() - start)
    # ...

# В settings_view() после сохранения:
from .metrics import POLICY_CHANGES_TOTAL
POLICY_CHANGES_TOTAL.inc()
```

**Шаг 4. Эндпоинт /metrics**

В `logger_app/urls.py`:

```python
from prometheus_client import make_wsgi_app
from django.urls import path
from django.http import HttpResponse
from prometheus_client import generate_latest, CONTENT_TYPE_LATEST

def metrics_view(request):
    return HttpResponse(generate_latest(), content_type=CONTENT_TYPE_LATEST)

urlpatterns = [
    # ... существующие маршруты ...
    path('metrics/', metrics_view, name='metrics'),
]
```

**Шаг 5. Конфигурация Prometheus**

Файл `prometheus.yml`:

```yaml
global:
  scrape_interval: 15s  # Сбор метрик каждые 15 секунд

scrape_configs:
  - job_name: 'security_logger'
    static_configs:
      - targets: ['localhost:5000']
    metrics_path: '/logger/metrics/'
```

**Шаг 6. Дашборд Grafana для показателей ГОСТ Р ИСО/МЭК 25010**

| Панель Grafana | Метрика Prometheus | Характеристика ГОСТ 25010 |
|---------------|-------------------|--------------------------|
| Доля разрешённых запросов | `rate(policy_checks_total{result="allowed"}[5m])` | Функциональная корректность |
| Время отклика дашборда (P95) | `histogram_quantile(0.95, request_duration_seconds)` | Временна́я эффективность |
| Текущее количество аномалий | `anomaly_count{type="critical"}` | Надёжность → Зрелость |
| Количество изменений политики | `increase(policy_changes_total[1h])` | Защищённость → Подотчётность |

**Результат:** Система мониторинга Prometheus+Grafana позволяет в реальном времени отслеживать 4 ключевые характеристики качества Security Logger согласно ГОСТ Р ИСО/МЭК 25010, обеспечивая количественную оценку функциональной корректности, производительности, надёжности и защищённости.

---

## 4. Краткие выводы

В ходе выполнения лабораторной работы №11 был проведён комплексный анализ качества программного средства «Контейнер журналирования событий доступа» (Security Logger).

**Основные результаты:**

1. **По ГОСТ Р ИСО/МЭК 12207:** Жизненный цикл Security Logger охватывает все ключевые процессы — от анализа требований до развёртывания. Автоматизированный скрипт `setup.sh` реализует процесс поставки, тестовые модули pytest — процесс тестирования, декораторы и политика в JSON — процесс управления рисками безопасности.

2. **Сравнительный анализ систем мониторинга:** Из 6 рассмотренных систем (Prometheus, Grafana, Zabbix, Nagios, Datadog, New Relic) для Security Logger оптимальна связка **Prometheus + Grafana** как полностью бесплатная, нативно интегрируемая с Python и ориентированная на метрики временны́х рядов.

3. **По ГОСТ Р ИСО/МЭК 25010:** Security Logger демонстрирует высокое качество по характеристикам защищённости (Fernet-шифрование, ролевое разграничение, CSRF-защита) и функциональной пригодности (все требования ТЗ реализованы). Области для улучшения — масштабируемость (1 worker Gunicorn) и переносимость (привязка к SQLite).

4. **Практическая настройка Prometheus:** Предложена реализация 4 метрик, покрывающих функциональную корректность, производительность, надёжность и подотчётность, с примерами PromQL-запросов для дашборда Grafana.

---

## 5. Список использованной литературы

1. ГОСТ Р ИСО/МЭК 25010-2015. Системная и программная инженерия. Требования и оценка качества систем и программного обеспечения (SQuaRE). Модели качества систем и программных продуктов. — М.: Стандартинформ, 2015.
2. ГОСТ Р ИСО/МЭК 12207-2010. Информационная технология. Системная и программная инженерия. Процессы жизненного цикла программных средств. — М.: Стандартинформ, 2011.
3. Prometheus Documentation. Overview. URL: https://prometheus.io/docs/introduction/overview/
4. Grafana Labs. Grafana Documentation. URL: https://grafana.com/docs/
5. Zabbix LLC. Zabbix Technical Overview. URL: https://www.zabbix.com/zabbix_overview
6. Nagios Enterprises. Nagios XI Documentation. URL: https://www.nagios.com/products/nagios-xi/
7. Datadog Inc. Datadog Documentation. URL: https://docs.datadoghq.com/
8. New Relic Inc. New Relic Documentation. URL: https://docs.newrelic.com/
9. Django Software Foundation. Django Documentation. URL: https://docs.djangoproject.com/
10. Корнеев Н.В. Задание №11. Качество программного обеспечения. — 2025.
